<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jingheng.a105project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".activity.SportReportActivity" />
        <activity android:name=".activity.BloodReportActivity" />
        <activity android:name=".activity.QAActivity" />
        <activity android:name=".activity.WaterActivity" />
        <activity android:name=".activity.SportActivity" />
        <activity android:name=".activity.LineGraphActivity" />
        <activity android:name=".activity.FoodActivity" />
        <activity android:name=".activity.DrugActivity" />
        <activity android:name=".activity.BloodpressureActivity" />
        <activity android:name=".activity.BloodActivity" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>